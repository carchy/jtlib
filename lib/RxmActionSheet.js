/**
 * RxmActionSheet插件
 * @see RxmActionSheet插件,菜单插件
 * @see RxmActionSheet插件使用示例
 * ```
   //插件引用
   define(['RxmActionSheet'],function(RxmActionSheet){
      
   }
 * ```
 * @version  1.4.3
 */
export default class RxmActionSheet {
    /**
     * 打开列表型菜单
     * @param arg  列表型菜单参数
     */
    openListSheet(arg) { throw (''); }
    /**
     * 打开表格型菜单
     * @param arg 表格型菜单参数
     */
    openGridSheet(arg) { throw (''); }
}
